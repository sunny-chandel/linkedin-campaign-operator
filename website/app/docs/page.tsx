import type { Metadata } from 'next';
import Link from 'next/link';
import { Footer, Header, PageHero, githubUrl } from '../components/Shell';

export const metadata: Metadata = { title: 'Claude LinkedIn Documentation', description: 'Documentation for Claude LinkedIn installation, configuration, state, architecture, testing, and contribution.', alternates: { canonical: '/docs' } };
export default function DocsPage() { return <main><Header /><PageHero eyebrow="Documentation" title="Understand the state. Control the campaign." lede="The operator is made from readable skill instructions, ordinary JSON state, and deterministic scripts." /><section className="docs-grid"><article className="doc-card"><h2>Quickstart</h2><p>Install, discover your profile, initialize state, validate it, and execute the first stage.</p><Link href="/install">Open installation →</Link></article><article className="doc-card"><h2>Architecture</h2><p>See how the orchestrator routes work across eight skills while campaign state stays portable.</p><a href={`${githubUrl}/blob/main/docs/ARCHITECTURE.md`}>Read architecture →</a></article><article className="doc-card"><h2>Examples</h2><p>Use prompts for founders, technical creators, consultants, operators, and content-only workflows.</p><Link href="/examples">Browse examples →</Link></article><article className="doc-card"><h2>Contribute</h2><p>Add adapters, scoring fixtures, campaign examples, status improvements, and new platform guides.</p><a href={`${githubUrl}/blob/main/CONTRIBUTING.md`}>Contribution guide →</a></article></section><section className="section"><p className="eyebrow">State model</p><h2>Versioned skills. Mutable learning.</h2><p className="section-lede">Campaign configuration, consent, queues, analytics, experiments, and logs live outside the plugin. Updates can improve the operator without erasing what your campaign learned.</p></section><Footer /></main>; }
